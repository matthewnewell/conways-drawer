/** Retired: the back link now lives in AppHeader's first slot, naming where it goes. Kept as a
 * no-op so an app that still renders it doesn't break while it moves to AppHeader. */
export default function DepotBackBar() {
    return null;
}
